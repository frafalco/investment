# Env file per docker-compose (opzionale)
# Copiala in `.env` nella root (stesso livello di docker-compose.yml) e personalizza.

# JWT secret: GENERA con `python -c "import secrets; print(secrets.token_hex(32))"`
JWT_SECRET=change-me-with-a-64-char-random-hex-string-1234567890abcdef
